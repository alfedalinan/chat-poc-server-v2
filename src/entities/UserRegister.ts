interface UserRegister {
    id?: number,
    userId?: number,
    registerId?: string,
    ipAddress?: string,
    ipPort?: string,
    createdAt?: string,
    updatedAt?: string,
    expiresAt?: string
}

export { UserRegister }